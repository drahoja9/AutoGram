//#region imports
import styled from 'styled-components';
//#endregion

/**
 * A `<p>` tag which makes it's text monospaced.
 */
const MonoText = styled.p`
  font-family: monospace;
`;

export default MonoText;
